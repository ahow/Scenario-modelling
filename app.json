{
  "name": "scenario-engine",
  "description": "Geopolitical scenario modelling engine — maps decision variables to conflict scenario probabilities and market impact",
  "stack": "heroku-24",
  "buildpacks": [
    { "url": "heroku/nodejs" }
  ],
  "env": {
    "NPM_CONFIG_PRODUCTION": {
      "value": "false",
      "description": "Install devDependencies so the build step can use Vite, esbuild, tsx etc."
    },
    "NODE_ENV": {
      "value": "production",
      "description": "Set to production at runtime"
    }
  }
}
